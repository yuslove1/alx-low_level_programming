#include "main.h"
/**
* _abs - absolute value of an integer
* @abs: function return type
* Return: Return abs if the condition is true and -abs if the false
*/
int _abs(int abs)
{

	if (abs >= 0)
	return (abs);
	else
	return (-abs);
}
