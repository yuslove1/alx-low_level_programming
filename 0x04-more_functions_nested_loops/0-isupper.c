#include "main.h"
/**
* _isupper - check for uppercase alphabet
* @c: parameter
* Return: return 1 if c is uppercase and 0 otherwise
*/

int _isupper(int c);
{
	int alpha;

	if (alpha >= 'A' && alpha <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
