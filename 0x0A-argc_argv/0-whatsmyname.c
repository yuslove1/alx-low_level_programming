#include <stdio.h>

/**
 * main - Entry of the function
 * @argc: the count
 * @argv: the array of string
 * Return: 0 always
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
