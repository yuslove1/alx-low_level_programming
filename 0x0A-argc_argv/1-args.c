#include <stdio.h>

/**
 * main - Entry of the function
 * @argc: the count
 * @argv: the array of string
 * Return: 0 always
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
